# 三个可交互 Demo + 作品集 · 同一国内域名

目标：HR 在 **`https://www.chenmengqiu-portfolio.xyz`** 上既能看你的主页，也能**直接体验**三个项目（不是只看截图）。

**不需要再买域名。** 所有路径都在一个域名下：

| 内容 | 地址 |
|------|------|
| 作品集 | `/` |
| UniPass 营销 + 演示 | `/demos/unipass/marketing/` |
| 本地出行 Agent（可点可玩，mock） | `/demos/meituan-agent/?demo=1` |
| joblens 落地页 | `/demos/joblens/` |

---

## 给访客 / HR 的说明（可写在简历或邮件里）

站点已使用国内可访问的自定义域名。若**完整产品交互**加载缓慢、预览空白或无法登录，通常是因为部分 Demo 仍依赖**海外服务器**，与国内网络环境有关，**不是链接失效**。

**建议访客尝试：**

1. 先浏览作品集主页与各项目的**营销页 / 介绍页**（国内一般可正常打开）。
2. 需要**完整可点击体验**时：切换手机热点、家用宽带，或使用 **VPN** 后刷新页面。
3. 页内若出现「海外完整版」按钮，可在 VPN 环境下打开该链接。

**各项目大致情况：**

| 项目 | 国内通常能看什么 | 可能需要 VPN 的部分 |
|------|------------------|---------------------|
| 作品集主页 | ✅ 全文、图片、简历下载 | — |
| 美团 Agent | ✅ 同域 mock 可交互 Demo | 一般不需要 |
| joblens | ✅ 产品介绍页 | Chrome 扩展安装与 Boss 页侧栏（本机操作） |
| UniPass | ✅ 营销故事页、版式与文案 | 页内**手机框实时 App 预览**、跳转「全站 App」 |

**可复制到邮件的简短话术：**

> 作品集：https://www.chenmengqiu-portfolio.xyz/  
> 说明：主站与项目介绍页国内可直接访问；若需体验完整交互 Demo，个别模块托管在海外，加载慢或空白时可尝试切换网络或使用 VPN。感谢耐心体验。

**可复制到简历脚注（一行）：**

> 在线 Demo 部分功能需良好国际网络或 VPN，营销页与 mock 演示国内可直接访问。

---

## 为什么现在 Vercel 反代不够？

反代仍走**海外**服务器，国内 iframe 容易白屏。要让 HR **稳定可交互**，需要：

1. **ICP 备案**（域名在阿里云，约 1～3 周）
2. 整站（含 `demos/`）放到 **阿里云 OSS + CDN（中国大陆）**
3. 域名解析从 Vercel **改到** 阿里云 CDN

---

## 第一步：构建三个项目到 `demos/`（本机）

在作品集目录打开 PowerShell：

```powershell
cd "d:\Users\86133\Desktop\cmq个人网站"
.\scripts\build-demos.ps1
```

脚本会：

- 复制 `joblens/website` → `demos/joblens/`
- 构建美团 Agent（**mock 模式**，国内无需 LLM API）→ `demos/meituan-agent/`
- UniPass：若 Next 静态导出报错（`app/api/*`），暂保留 Vercel 反代；要纯国内需先改 API 路由或单独导出营销页

构建成功后，双击 **`推送GitHub.bat`**（可选：过渡期仍走 Vercel，有本地文件会**优先**于反代）。

---

## 第二步：阿里云备案

1. 阿里云控制台 → **备案**
2. 主体：个人；网站：作品集展示
3. 备案域名：`chenmengqiu-portfolio.xyz`
4. 等审核通过（期间可继续用 Vercel + 自定义域名）

---

## 第三步：OSS 托管整站

### 1. 创建 Bucket

- 地域：**华东**等大陆节点（如 `oss-cn-hangzhou`）
- 读写权限：公共读（或通过 CDN 回源私有桶）
- 开启 **静态网站托管**，默认首页 `index.html`，404 填 `index.html`（作品集单页需要）

### 2. 上传文件

将 **`cmq个人网站` 整个目录**（含 `index.html`、`assets/`、`demos/`、`css/`、`js/`）上传到 Bucket **根目录**。

可用脚本（需先安装 [ossutil](https://help.aliyun.com/zh/oss/developer-reference/ossutil/) 并配置 AccessKey）：

```powershell
.\scripts\sync-oss.ps1 -Bucket "你的bucket名称"
```

或在控制台 **文件管理** 里拖拽上传。

### 3. 绑定 CDN + 域名

1. 开通 **CDN**，源站选该 OSS Bucket
2. 备案通过后，CDN 添加加速域名：`www.chenmengqiu-portfolio.xyz`
3. **域名解析**（阿里云 DNS）：
   - 删除指向 Vercel 的 `www` CNAME / `@` A 记录
   - 按 CDN 控制台提示添加 **CNAME** 到 CDN 域名

### 4. HTTPS

在 CDN 申请免费 SSL 证书，开启 HTTPS 强制跳转。

---

## 第四步：验证 HR 可体验

打开并亲自点一遍：

- [ ] `https://www.chenmengqiu-portfolio.xyz/`
- [ ] 首页作品区 iframe 有画面
- [ ] `/demos/meituan-agent/?demo=1` — 输入一句话能出规划（mock）
- [ ] `/demos/unipass/marketing/` — 营销页滚动、演示区正常
- [ ] `/demos/joblens/` — 落地页正常

---

## 各项目「可交互」说明

| 项目 | 国内 OSS 上能玩到什么程度 | VPN 说明 |
|------|---------------------------|----------|
| **美团 Agent** | ✅ 默认 **mock**，规划/交付流程可完整点（无需海外 API） | 通常**不需要** VPN |
| **joblens 落地页** | ✅ 浏览、下载链接；**Chrome 扩展**需 HR 本机安装（侧栏在 Boss 页） | 落地页一般**不需要** VPN |
| **UniPass** | ✅ 营销页静态页国内可看 | 页内 **App 实时预览** 指向 `unipass-nu.vercel.app`，国内常需 **VPN** 或「海外完整版」链接 |

若某项目强依赖**海外 API**，需要把 API 也迁到国内（阿里云函数/ECS），或保留「海外完整版」备用链接（作品集已配置 iframe 兜底与按钮文案）。

---

## 更新流程（以后改项目）

1. 改对应项目代码 → 再跑 `.\scripts\build-demos.ps1`
2. `推送GitHub.bat`（若仍用 Vercel 备份）或 `.\scripts\sync-oss.ps1` 上传 OSS
3. CDN 刷新缓存（控制台 **刷新预热**）

---

## 常见问题

**Q：还要给三个项目各绑域名吗？**  
A：不要。一个域名 + `/demos/子路径` 即可。

**Q：备案期间能用吗？**  
A：可以。继续用 `www.chenmengqiu-portfolio.xyz` → Vercel；推送含 `demos/` 的构建后，Vercel 会优先托管静态 Demo（仍可能偏慢）。

**Q：解析改到阿里云后 Vercel 还能用吗？**  
A：可以留作你自己预览；简历只写国内域名。

**Q：要不要在简历里写「需 VPN」？**  
A：建议写一句简短说明（见上文「可复制到简历脚注」），降低 HR 误以为链接坏了的概率；备案 + 全站 OSS 上线后，可再缩短或去掉这句。
